  name: Build APK

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.0'
          channel: 'stable'

      - name: Get Flutter dependencies
        working-directory: ./ou_ai
        run: flutter pub get

      - name: Build APK (Debug)
        working-directory: ./ou_ai
        run: flutter build apk --debug

      - name: Build APK (Release)
        working-directory: ./ou_ai
        run: flutter build apk --release

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: APK-builds
          path: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}      working-directory: ./ou_ai
        run: flutter build apk --debug

      - name: Build APK (Release)
        working-directory: ./ou_ai
        run: flutter build apk --release

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: APK-builds
          path: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Get Flutter dependencies
        working-directory: ./ou_ai
        run: flutter pub get

      - name: Build APK (Debug)
        working-directory: ./ou_ai
        run: flutter build apk --debug

      - name: Build APK (Release)
        working-directory: ./ou_ai
        run: flutter build apk --release

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: APK-builds
          path: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}      - name: Get Flutter dependencies
        run: |
          cd ou_ai
          flutter pub get

      - name: Build APK (Debug)
        run: |
          cd ou_ai
          flutter build apk --debug

      - name: Build APK (Release)
        run: |
          cd ou_ai
          flutter build apk --release

      - name: Upload APK artifacts
        uses: actions/upload-artifact@v4
        with:
          name: APK-builds
          path: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
          retention-days: 30

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          files: |
            ou_ai/build/app/outputs/apk/debug/app-debug.apk
            ou_ai/build/app/outputs/apk/release/app-release.apk
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
